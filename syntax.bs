#include libs/stdlib.bs
#include libs/os.bs
#include libs/list.bs

block testFunc int -> int
    a
    if a == 0
    | return 1 
    return 0
end

block main -> int
    x = 0
    if testFunc x == 0
    | println "YAY!"
    if testFunc x == 1
    | println "NOO!"
    return 0
end