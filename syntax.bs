#include libs/stdlib.bs
#include libs/os.bs
#include libs/list.bs
block main -> int
    x = generator 0 10
    i = 0
    do 
        op = li_get x i
        stdout_i op
        stdout "\n"
        i + 1
    while i < 11
    size = li_size x
    stdout_i size 
    stdout "\n"
    return 0
end