#include libs/stdlib.bs
#include libs/os.bs

block main -> int
    a = iprompt "Number: "
    if a == 10
    | stdout "Hello world!\n"
    | return 0
    stdout "Hello I am evil!\n"
    return 0
end